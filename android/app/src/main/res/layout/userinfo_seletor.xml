<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:padding="15dip" >

    <TextView
        android:id="@+id/curusers_users_note"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dip"
        android:text="@string/curusers_user"
        android:textColor="@color/settings_key"
        android:textSize="22sp"
        android:textStyle="bold"
        android:visibility="gone" />

    <GridView
        android:id="@+id/curusers_users_data"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:horizontalSpacing="5dip"
        android:listSelector="#00000000"
        android:numColumns="auto_fit"
        android:padding="10dip"
        android:scrollbars="none"
        android:stretchMode="columnWidth"
        android:verticalSpacing="5dip"
        android:visibility="gone" />

    <TextView
        android:layout_width="fill_parent"
        android:layout_height="30dip" />
        
    <TextView
        android:id="@+id/curusers_roles_note"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dip"
        android:text="@string/curusers_role"
        android:textColor="@color/settings_key"
        android:textSize="22sp"
        android:textStyle="bold"
        android:visibility="gone" />

    <GridView
        android:id="@+id/curusers_roles_data"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:horizontalSpacing="5dip"
        android:listSelector="#00000000"
        android:numColumns="auto_fit"
        android:padding="5dip"
        android:scrollbars="none"
        android:stretchMode="columnWidth"
        android:verticalSpacing="5dip"
        android:visibility="gone" />

</LinearLayout>